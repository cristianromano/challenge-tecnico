# 📝 Task Manager App

Una aplicación web para la gestión de tareas, desarrollada con **React** y **TypeScript**. Permite crear, editar y eliminar tareas, persistiendo los datos en el navegador y ofreciendo soporte para cambio de idioma en tiempo real (Español/Inglés).

## 🚀 Tecnologías Utilizadas

- **React** (Hooks usados: `useState`, `useEffect`)
- **TypeScript** (Tipado estático y modularidad)
- **Tailwind CSS** (Estilos modernos y responsivos)
- **React-i18next** (Internacionalización y gestión de idiomas)
- **LocalStorage** (Persistencia de datos en el cliente)
- **Vite** (Entorno de desarrollo y bundler)

## ✨ Funcionalidades

- ✅ **CRUD Completo:** Crear, Leer, Actualizar y Eliminar tareas.
- 🌍 **Multi-idioma:** Cambio dinámico entre Español e Inglés sin recargar la página.
- 💾 **Persistencia:** Las tareas no se pierden al cerrar el navegador (LocalStorage).
- 🎨 **UI/UX:** Diseño limpio tipo "tarjeta" con feedback visual.

## 🛠️ Instalación y Configuración

Sigue estos pasos para correr el proyecto en tu entorno local:

### 1. Prerrequisitos

Asegúrate de tener instalado [Node.js](https://nodejs.org/) (versión 14 o superior).

### 2. Clonar el repositorio

```bash
git clone https://github.com/cristianromano/challenge-tecnico.git
```

### 3. Entrar a la carpeta frontend y instalar dependencias

```
npm install
```

### 4. Ejecutar el servidor de desarrollo

```
npm run dev
```

La aplicación estará disponible en: http://localhost:5173 (o el puerto que te indique la terminal).
